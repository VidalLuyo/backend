apiVersion: apps/v1
kind: Deployment
metadata:
  name: vg-ms-institution
  namespace: jesus-luyo
  labels:
    app: vg-ms-institution  # Debe coincidir con Service selector
spec:
  replicas: 2  # Número de pods
  selector:
    matchLabels:
      app: vg-ms-institution
  template:
    metadata:
      labels:
        app: vg-ms-institution
    spec:
      containers:
      - name: app
        image: vidalluyo0/vg-ms-institution:1.0  # Cambiar versión aquí
        imagePullPolicy: Always
        ports:
        - containerPort: 9080
        env:
        - name: SERVER_PORT
          value: "9080"
        resources:
          requests:  # Mínimo garantizado
            memory: "512Mi"
            cpu: "250m"
          limits:  # Máximo permitido
            memory: "1Gi"
            cpu: "500m"
